---
const links = [
{ title: 'Home', path: '/#' },
{ title: 'About', path: '/#end-home' },
{ title: 'Archives', path: '/#end-about-me' },
{ title: 'Projects List', path: '/projects' },
{ title: 'Contact', path: '/#end-archives' },
]
---
<nav>
  <ul>
    {
      links.map(({title, path}) => (
        <li>
          <a href={path}>{title}</a>
        </li>
      ))
    }
  </ul>
</nav>

<style lang="scss">
  nav {
    ul {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: center;
      list-style: none;
      gap: 1rem;
      li {
        a {
          text-decoration: none;
          color: var(--color-text);
        }
      }
    }
  }
</style>